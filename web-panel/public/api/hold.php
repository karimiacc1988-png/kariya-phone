<?php
require __DIR__ . '/_lib.php';

// نگه‌داشتن/برگرداندن از انتظار.
// نیازمند فلگ‌های بیشتر AMI است؛ در این نسخه فقط تأیید نرم می‌دهیم تا رابط نشکند.
// پیاده‌سازی کامل با Action: Park یا Hold در فاز بعد.
$in = input();
$id = (string)($in['id'] ?? '');
if ($id === '') fail('شناسه تماس لازم است');

out(['ok' => true, 'state' => 'hold', 'note' => 'انتظار در فاز بعد کامل می‌شود']);
