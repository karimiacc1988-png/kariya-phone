<?php
require __DIR__ . '/_lib.php';

// پاسخ به تماس در معماری AMI معمولاً از خود گوشی/سافت‌فون انجام می‌شود،
// نه از سرور. اینجا تأیید نرم می‌دهیم تا رابط یکدست بماند.
$in = input();
$id = (string)($in['id'] ?? '');
if ($id === '') fail('شناسه تماس لازم است');

out(['ok' => true, 'note' => 'پاسخ از روی گوشی/زویپر داده می‌شود']);
